<!DOCTYPE html>
<html :class="{ dark: theme.dark }" x-data="app()" lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <meta name="theme-color" content="#ffffff">

  @entryPointStyles('app')
  @entryPointScripts('app')
</head>
<body>
  <div class="drawer drawer-mobile h-screen bg-gray-50 dark:bg-gray-900" x-data="{ open: false }" @click.outside="open = false">
    <input type="checkbox" :checked="open" class="drawer-toggle" />
    <div class="drawer-content">
      @include('partials/header')

      <div class="flex flex-col justify-center px-4" style="z-index: 9999">
        @!section('body')
      </div>
  </div>
  @include('partials/sidemenu')
</div>
</body>
</html>
